version: 0.2

phases:
  build:
    commands:
      - echo "Emulating Build"

artifacts:
  files:
    - app/**/*
    - appspec.yml
    - deploy/**/*
    - default
  secondary_artifacts:
    js:
      files:
        - js/*